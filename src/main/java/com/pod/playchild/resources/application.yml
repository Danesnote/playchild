spring:
  application:
    name: playchild
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 1234
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
